// Define constant for server IP address
const IP = "localhost";

// Define constant for server port number
const PORT = 50541;

// Export constants to make available in other modules
module.exports = {
  IP,
  PORT,
};
